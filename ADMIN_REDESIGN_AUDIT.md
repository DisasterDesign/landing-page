# דוח אפיון ושיפורים — מערכת הניהול (Admin)

מחקר מקצה-לקצה של כל מסכי הניהול: theming/dark-mode, בעיות UX, וכפילויות/אי-בהירות במידע.
תאריך: יוני 2026.

---

## 0. ממצא תשתיתי — איך עוברים ל-Dark Mode (וזה קל ממה שנראה)

כל מערכת הניהול **כבר כתובה במחלקות dark** (`bg-gray-900`, `text-white`, `bg-pink`, `text-cyan`). היא נראית בהירה היום רק בגלל בלוק אחד ב-`src/app/globals.css` בשם `.admin-light` (שורות ~147-330) שעושה שני דברים:
1. ממפה את האפורים הכהים לבהירים (`bg-gray-900` → לבן וכו').
2. **מנטרל את צבעי המותג** — הופך `--color-pink` ו-`--color-cyan` לאפור.

**מסקנה:** מעבר ל-dark = הסרת/היפוך הבלוק הזה + החזרת המותג. שינוי אחד מעצב מחדש את **כל** המסכים בעקביות. צריך לתקן רק קומפוננטות בודדות שכתובות "בהיר" (`ui/Card.tsx` = `bg-white`, `ui/Button.tsx` ו-`ui/Input.tsx` = `text-black`) ואת ה-hex הקשיחים בגרפים (recharts).

**סיכוני dark-mode שזוהו:**
- `RevenueChart.tsx` ו-`seo/page.tsx` — צבעי גרף קשיחים (`#111827`, `#e5e7eb`) שיהיו **בלתי נראים** על רקע כהה. דורש החלפה לצבעי מותג/בהירים.
- `ui/Card.tsx` (`bg-white`), `ui/Button.tsx`/`ui/Input.tsx` (`text-black`) — ייראו שבורים.
- ניגודיות: `text-cyan` (`#01FFFF`) כטקסט קטן על אפור כהה נכשל בניגודיות — צריך cyan מעט מעומעם.
- ה-root layouts (`admin/layout.tsx`, `seller/layout.tsx`) קשיחים `bg-white text-gray-900` — חייב להשתנות לכהה.

---

## 1. הבעיה הכי חשובה — נכונות פיננסית (לא רק עיצוב) ⚠️

**`Client.amount` הוא מצבר (accumulator), אבל מוצג כסכום חודשי.** ה-webhook מבצע `increment` על השדה בכל תשלום (חיוב ראשון + כל חיוב חוזר). כלומר אחרי 6 חודשים, `amount` של לקוח ≈ פי 6 מהמחיר החודשי האמיתי.

איפה זה שובר דברים:
- **`/admin/clients`** — הכותרת "סכום (כולל מע״מ)" ועמודות המע״מ/רווח מחושבות מהמצבר → **מנופחות פי החודשים**. ה"סה״כ" בתחתית הוא לא MRR אלא סכום-חיים מצטבר.
- **`/admin/partner-report`** — מתייחס ל-`amount` כ"חודשי" ומחשב ממנו רווח וחלוקת שותפים. **ככל שלקוח משלם יותר חודשים, חלקו של כל שותף מנופח** — חלוקת הרווח בין אלעד לרועי שגויה מתמטית מהחיוב השני ואילך.

זו לא בעיית label — זו בעיית כסף אמיתית בין השותפים. **דורשת החלטה: לתקן את מקור האמת (להפריד "MRR חודשי" מ"סך התקבולים"), ולתקן את חישוב ה-partner-report.**

קונפליקט נוסף: **שלוש מוסכמות מע״מ במקביל** — הסכמים אומרים "monthlyPrice לפני מע״מ", לקוחות אומר "כולל מע״מ", partner-report מחלק ב-118. אותו לקוח "59 ₪" מוצג שונה בכל מסך.

---

## 2. כפל איזורים ואי-בהירות במידע (העדיפות שהדגשת)

### 2.1 לידים מול הודעות — אותה טבלה, שני מסכים
`/admin/leads` ("לידים") ו-`/admin/contacts` ("הודעות") כותבים **לאותה טבלה** (`ContactSubmission`) ועם **אותו enum סטטוס**. הם לא שני מקורות — הם שני UI על אותו דבר.
- שינוי סטטוס במסך אחד משנה בשקט את השני.
- סגירת ליד מ-"הודעות" **לא** מסמנת `closedAt` (רק מ-"לידים") → הליד לא נספר ב"נסגרו החודש".
- "הודעות" תקוע על 50 שורות (pagination נזרק); "לידים" טוען 200. שתי "אמיתות" שונות.
- מחיקה/bulk קיימים רק ב-"הודעות"; notes/אחראים/מעקב רק ב-"לידים". המשתמש נאלץ לקפוץ ביניהם.
- **המלצה:** למזג למסך אחד ("פניות / לידים") שמשלב inbox + CRM, ולנתב את כל כתיבות הסטטוס דרך handler אחד.

### 2.2 צביר הפיננסי — 4 מסכים, אותו כסף בהגדרות שונות
`clients` / `agreements` / `partner-report` / `sellers` — כולם מציגים כסף, כל אחד ממקור והגדרה אחרת של "סכום". מעבר לבאג ב-1: `clients` ו-`partner-report` משכפלים את **אותו** חישוב מע״מ/עמלה/רווח בשתי מימושים (אחד client-side באגי שמתעלם מ-`vatExempt`, אחד server-side תקין). partner-report מציג את אותו מספר פעמיים ("חלק אלעד"/"חלק רועי").

### 2.3 בלוג — שתי מערכות סטטוס במקביל
`BlogPost` יש לו גם `published` (בוליאני) וגם `status` (enum 5-מצבים). מסך `/admin/blog` מציג בינארי ("טיוטה"/"פורסם"); `/admin/blog/pipeline` מציג 5 מצבים. המילה "טיוטה" אומרת שני דברים שונים בין המסכים. צריך מקור-אמת אחד.

### 2.4 קטנים אך ברורים
- **Settings** — `roleLabels` לא מכיר `SELLER` → מוכר מוצג בטעות כ"חבר צוות". (באג)
- **SEO** — תוויות תת-הניווט לא תואמות לכותרות העמודים; מסך הסקירה בלי כותרת.
- **Shell** — לוגיקת initials/שם/avatar/logout משוכפלת; שני פונטים (Heebo + Birzia) במקביל; כותרת "ניהול" קבועה בלי breadcrumb.

---

## 3. הדשבורד — לעצב מחדש סביב נתונים (הבקשה המפורשת)

הדשבורד היום הוא מסך-משימות אישי, לא מסך-עסק. לפי `strategy.md` הוא אמור לענות "איפה אנחנו מול היעדים".

**מוצע (3 שכבות):**
- **באנר יעד:** לקוחות פעילים מול 100 (פס התקדמות, חודשים שנותרו), חדשים החודש מול התוכנית.
- **שורת KPI:** MRR, רווח חודשי, חלק כל שותף, לידים פתוחים, עמלות מוכרים ממתינות. **כל אלה מ-API קיים** (`partner-report`, `leads`, `sellers/commissions`) — מקור אחד, בלי כפל-חישוב.
- **מגמות:** גרף הכנסה/רווח (recharts, dark-themed), ויחס Bet/Floor.
- **אופס:** המשימות שלי (כמו היום).

KPI שאין להם נתונים כיום (דורשים תוספת schema, לא להמציא): **זמן בנייה לחנות** ו-**churn**.

---

## 4. סדר עבודה מוצע
1. **Dark mode + design system** — נטרול `.admin-light` לכהה, תיקון קומפוננטות בהירות, ריענון tokens, גרפים. (מעצב את כל המסכים בבת אחת)
2. **דשבורד חדש** מבוסס-נתונים מ-partner-report (מקור אחד).
3. **תיקוני בהירות בטוחים:** מיזוג לידים/הודעות, איחוד סטטוס בלוג, SELLER ב-settings, תוויות SEO.
4. **באג פיננסי (1)** — נפרד ורגיש (משפיע על חלוקת רווח): הפרדת MRR ממצבר תקבולים + תיקון partner-report.
